<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <me.hongui.learngerman.view.TextProgressBar
        android:id="@+id/sb_item_course_progress"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/dp16"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="@+id/tv_item_course_title"
        app:layout_constraintBottom_toBottomOf="@+id/tv_item_course_title"
        app:layout_constraintRight_toLeftOf="@+id/tv_item_course_title"/>

    <TextView
        android:id="@+id/tv_item_course_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/dp16"
        android:paddingBottom="@dimen/dp16"
        android:ems="5"
        android:gravity="right"
        app:layout_constraintLeft_toRightOf="@+id/sb_item_course_progress"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginRight="@dimen/dp8"
        android:layout_marginLeft="@dimen/dp4"/>

</androidx.constraintlayout.widget.ConstraintLayout>